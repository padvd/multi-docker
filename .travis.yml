language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t padvd/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true padvd/react-test npm test

after_success:
  - docker build -t padvd/multi-client ./client
  - docker build -t padvd/multi-nginx ./nginx
  - docker build -t padvd/multi-server ./server
  - docker build -t padvd/multi-worker ./worker
  # Log in to the Docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push padvd/multi-client
  - docker push padvd/multi-nginx
  - docker push padvd/multi-server
  - docker push padvd/multi-worker



